<template>
    <div class="<%= data.lowerName %>">
      <base-table
        apiUrl="/getData"
        :tableConfig="tableConfig"
        :formConfig="formConfig"
        :formRules="formRules"
        :tool="tool"
        :toolbar="toolbar"
        :width="100"
        :checkBox="false"
        :number="false"
        saveUrl="saveUser"
      >
        
        <!--操作按钮  -->
        <!-- <template #toolBtns="{ row }">
        </template> -->
         
      </base-table>
    </div>
  </template>
  
  <script>
  import BaseTable from "@/components/basetable/index";
  import TreeTable from "@/components/treetable/index";
  export default {
    name: "<%= data.name %>",
    components: {
      BaseTable,
      TreeTable,
    },
    data() {
      return {
        showEdit: false,
        selectRow:true,
        submitLoading: false,
        tableConfig: {
          columns: ["id", "email", "role", "releUser", "status","phone", "name", ],
        },
        formRules: {
          name: [
            { required: true, message: "请输入名称" },
            { min: 2, max: 5, message: "长度在 2 到 5 个字符" },
          ],
        },
        formConfig: {
          fields: ["Basic", "name", "phone", "email", "role"],
        },
        toolbar: {
          add: true,
        },
        tool: {
          edit: true,
        },
      };
    },
    methods: {
      editEvent(row) {
        console.log(row)
        this.showEdit = true;
      },
    },
  };
  </script>
  
  <style lang="scss" scoped>
  .<%= data.lowerName %> {
    padding-left: 10px;
    box-sizing: border-box;
    .tableContainer {
      width: 100%;
      // height: 91.6vh;
    }
    .sortable-column-demo .vxe-header--row .vxe-header--column.sortable-ghost,
    .sortable-column-demo .vxe-header--row .vxe-header--column.sortable-chosen {
      background-color: #dfecfb;
    }
  
    .el-button--primary.dark {
      color: #fff;
      background-color: #0f4980;
      border-color: #0f4980;
    }
  
    .el-button--primary.light {
      color: #fff;
      background-color: #1164b4;
      border-color: #1164b4;
    }
  }
  </style>